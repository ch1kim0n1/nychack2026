# Base URL of the CivicLens backend API (Railway). Must be set in the Vercel
# project's Environment Variables (Production/Preview), and locally in
# .env.local when running `npm run dev` against a remote backend.
NEXT_PUBLIC_API_URL=https://civiclens-backend.up.railway.app
